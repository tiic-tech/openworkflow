schema_version: 0.1.0
contract_id: atom_tasks:S001-skill-lifecycle-contract
contract_type: planning
planning_artifact_type: atom_tasks
selected_change_id: S001-skill-lifecycle-contract
title: Atom tasks for native skill lifecycle contract
status: ready
tasks:
  - task_id: T001
    title: Inspect current generated skill and registry sources
    status: pending
    type: read
    owned_paths:
      - packages/adapters/codex/src/generateSkills.ts
      - packages/adapters/codex/src/generateCommands.ts
      - packages/core/src/commands/registry.ts
      - packages/core/src/artifacts/registry.ts
      - .agents/skills/ow-proto/SKILL.md
    done_when:
      - Current OW skill format and generation source boundaries are confirmed.
      - OpenSpec comparison facts needed by the reference are identified without copying external code.
  - task_id: T002
    title: Write native skill lifecycle reference
    status: pending
    type: document
    owned_paths:
      - references/skill-system-lifecycle.md
    done_when:
      - Reference defines OW skill file shape, frontmatter policy, XML-like protocol block taxonomy, lifecycle ownership, and non-goals.
      - Reference explicitly distinguishes OW's static runtime skill contract from OpenSpec's dynamic artifact instruction envelopes.
  - task_id: T003
    title: Link planning contracts to skill lifecycle rules
    status: pending
    type: document
    owned_paths:
      - references/planning-artifact-contracts.md
    done_when:
      - Planning references point future generator and validator work to the native skill lifecycle contract.
      - No generated surfaces are edited.
  - task_id: T004
    title: Verify repository contracts
    status: pending
    type: verify
    owned_paths:
      - references/skill-system-lifecycle.md
      - references/planning-artifact-contracts.md
    done_when:
      - npm run validate passes.
      - git diff --check passes.
