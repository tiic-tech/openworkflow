schema_version: 0.1.0
contract_id: atom_tasks:G003-select-branch-dirty-guards
contract_type: planning
planning_artifact_type: atom_tasks
selected_change_id: G003-select-branch-dirty-guards
title: Atom tasks for select-change branch and dirty-tree guards
status: done
tasks:
  - task_id: T001
    title: Add branch and dirty-tree workflow checks
    status: done
    type: edit
    owned_paths:
      - skills/select-change/SKILL.md
    done_when:
      - The skill checks queue branch boundary before selection.
      - The skill identifies dirty-tree contamination before creating selection artifacts.
      - The skill does not perform git mutations.
  - task_id: T002
    title: Add protocol details for selection guards
    status: done
    type: edit
    owned_paths:
      - skills/select-change/references/selection-protocol.md
    done_when:
      - Branch mismatch and dirty tree handling are documented.
      - Commit boundary expectations are documented.
  - task_id: T003
    title: Update queue evidence
    status: done
    type: document
    owned_paths:
      - changes/M71-git-version-control-governance/
    done_when:
      - G003 selection and completion evidence are recorded.
      - G004 becomes ready after G002 and G003 are complete.
  - task_id: T004
    title: Validate select-change and repository
    status: done
    type: verify
    owned_paths:
      - skills/select-change/
      - changes/M71-git-version-control-governance/
    done_when:
      - quick_validate.py passes for select-change.
      - npm run validate passes.
      - git diff --check passes.
