schema_version: 0.1.0
contract_id: work_items:M33-summary-refresh-command
contract_type: work_items
title: M33 Summary refresh command work items
status: completed
change_contract: changes/M33-summary-refresh-command/CHANGE.yaml
items:
  - task_id: M33-T001
    title: Define deterministic summary generation model
    status: completed
    owned_paths:
      - packages/cli/src/commands/summarize.ts
      - packages/core/src/workflow/summaryHealth.ts
    acceptance:
      - Model maps source artifact paths to SUMMARY.yaml paths using summary_policy.
      - Generated summary includes artifact identity, status, source path, extracted key fields, and generated metadata.
      - Model is deterministic and does not read raw evidence by default.
  - task_id: M33-T002
    title: Implement summarize CLI command
    status: completed
    owned_paths:
      - packages/cli/src/index.ts
      - packages/cli/src/commands/summarize.ts
    acceptance:
      - Dry-run reports planned writes.
      - "`--write` creates or updates SUMMARY.yaml."
      - "`--artifact` and `--all` are supported with clear errors for invalid combinations."
  - task_id: M33-T003
    title: Preserve source artifacts and lazy boundaries
    status: completed
    owned_paths:
      - packages/cli/src/commands/summarize.ts
      - packages/cli/src/dev/verifyRuntimeSurface.ts
    acceptance:
      - Source artifacts, CURRENT_STATE.yaml, indexes, and raw evidence remain unchanged.
      - No missing stage directories are created by summarize.
      - current_slice contracts are skipped with guidance.
  - task_id: M33-T004
    title: Update Agent-facing docs
    status: completed
    owned_paths:
      - packages/cli/src/index.ts
      - packages/core/src/onboarding/agentsGuide.ts
    acceptance:
      - Help documents summarize, dry-run default, and `--write`.
      - AGENTS.md managed block tells Agents to use summarize only to refresh SUMMARY.yaml.
      - Existing inspect/summaries guidance remains consistent.
  - task_id: M33-T005
    title: Add summary refresh regression coverage
    status: completed
    owned_paths:
      - packages/cli/src/dev/verifyRuntimeSurface.ts
      - packages/cli/src/dev/verifyWorkflowE2E.ts
      - packages/core/src/validators/validateRepositoryContracts.ts
    acceptance:
      - Tests cover dry-run, write, missing summary, stale summary, current summary skip, and non-OpenWorkflow root.
      - Tests verify `summaries --json` reports current after summarize writes.
      - Repository validation includes M33 change contracts and new source files.
