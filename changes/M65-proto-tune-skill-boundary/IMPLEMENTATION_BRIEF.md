# M65 Implementation Brief

Split the prototype source skills before dogfood:

- `build-prototype` owns `/ow:proto` first-pass strategic exploration from
  vision or validation context.
- `tune-prototype` owns `/ow:tune` refinement from accepted baseline screens,
  prompt packs, and user feedback.

This is a temporary queue insertion before P005. The implementation must update
the queue through stable ids and operation entries instead of rewriting P005's
meaning.
