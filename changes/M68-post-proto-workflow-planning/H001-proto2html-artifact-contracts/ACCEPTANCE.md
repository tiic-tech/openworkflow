# H001 Acceptance

- [x] Proto2html artifacts require an accepted benchmark image or screen group input.
- [x] HTML output, screenshot evidence, fidelity gaps, and known limits are structured.
- [x] Proto2html is explicitly reconstruction-only, not product exploration or spec extraction.
- [x] `npm run validate` passes.
