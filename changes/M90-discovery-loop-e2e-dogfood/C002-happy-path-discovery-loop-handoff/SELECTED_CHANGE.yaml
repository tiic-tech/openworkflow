schema_version: 0.1.0
contract_id: selected_change:M90-discovery-loop-e2e-dogfood:C002
contract_type: planning
planning_artifact_type: selected_change
plan_id: M90-discovery-loop-e2e-dogfood
candidate_id: C002
change_id: C002-happy-path-discovery-loop-handoff
title: Verify happy-path discovery-loop command handoff
status: completed
selected_at: 2026-05-22
selected_by: agent
risk: medium
size: medium
selection_source:
  queue: changes/M90-discovery-loop-e2e-dogfood/CANDIDATE_CHANGES.yaml
selection_reason: >
  Select C002 through ow-select-change single-queue ranking. C001 completed the
  canonical fixture chain, and C002 is the next ready candidate to prove
  generated command surfaces and context packets can guide an agent through the
  happy-path discovery loop.
owned_paths:
  - packages/cli/src/dev/verifyWorkflowE2E.ts
  - packages/cli/src/dev/verifyRuntimeSurface.ts
  - changes/M90-discovery-loop-e2e-dogfood/
dependencies:
  - C001
scope:
  includes:
    - generated skill and context packet checks for the full discovery loop
    - current pointer/read-order expectations for the fixture chain
    - positive handoff assertions from vision through tune and decision audit
    - command readiness checks where the CLI supports them
  excludes:
    - negative failure routing
    - visual prototype inspection
    - async subagent orchestration
    - proto2html or downstream commands
forbidden_paths:
  - real image generation
  - visual review tooling
  - async subagent runtime behavior
  - proto2html or downstream command artifacts
acceptance:
  - Generated command surfaces expose the expected discovery-loop order.
  - Context packets contain enough optional/required context for agents to consume the next artifact.
  - Happy-path fixture handoff reaches benchmark-selection readiness without entering proto2html.
  - The checks fail if a command loses its generated handoff or artifact boundary.
validation:
  - npm run build
  - npm run validate
  - npm run verify:e2e-workflow
  - npm run verify:runtime-surface
  - node dist/cli/src/index.js validate --root . --json
  - git diff --check
high_risk:
  approval_required: false
  reason: >
    C002 is medium risk because it adds local generated-surface and fixture
    handoff verification only. It does not run external image generation or
    introduce async runtime behavior.
completion:
  completed_at: 2026-05-22
  commit: c524446
  evidence:
    - changes/M90-discovery-loop-e2e-dogfood/C002-happy-path-discovery-loop-handoff/LOCAL_COMMIT_EVIDENCE.yaml
