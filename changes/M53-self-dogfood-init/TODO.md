# M53 TODO

1. [x] Confirm PR #3 is merged and local `main` is up to date.
2. [x] Create `codex/m53-self-dogfood-init` from latest `main`.
3. [ ] Run `npm run build`.
4. [ ] Run local init with `node dist/cli/src/index.js init . --tools codex --json`.
5. [ ] Inspect generated `.openworkflow/`, `.agents/`, and AGENTS.md surfaces.
6. [ ] Classify source artifacts versus runtime/cache artifacts.
7. [ ] Run `doctor --json`, `handoff --json`, and `context --handoff --json`.
8. [ ] Commit the M53 bootstrap change.
