# Acceptance

- [x] Tune behavior preserves product system and accepted baseline screens.
- [x] Every refined prompt must be bound to target and source screen IDs.
- [x] `MUST_INHERIT`, `MUST_ADD`, `MUST_REMOVE`, and `FLEXIBLE_CHANGE` are explicit.
- [x] Requested removals are represented in negative constraints and acceptance checks.
- [x] Runtime exposure and HTML conversion remain out of scope.
