# M28 TODO

1. [x] Define shared JSON report envelope and helpers.
2. [x] Add JSON output to `brief` and `status` using the shared envelope.
3. [x] Add JSON output to `doctor` and `validate`.
4. [x] Add JSON output to `init`, `sync`, and `clean`.
5. [x] Update `openworkflow --help` and AGENTS.md managed block for JSON mode.
6. [x] Add regression tests that parse JSON for every command.
7. [x] Run validation suite.
