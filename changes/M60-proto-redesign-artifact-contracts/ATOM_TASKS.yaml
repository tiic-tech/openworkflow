schema_version: 0.1.0
contract_id: atom_tasks:M60-proto-redesign-artifact-contracts
contract_type: planning
planning_artifact_type: atom_tasks
selected_change_id: M60-proto-redesign-artifact-contracts
title: M60 Proto redesign artifact contract atom tasks
status: completed
tasks:
  - task_id: M60-A001
    title: Read prototype prompt source material
    status: completed
    type: read
    owned_paths:
      - /Users/archy/Downloads/vision_to_strategic_prototype_prompt_skill.zip
      - /Users/archy/Downloads/prototype_tune_to_refined_prompt_skill.zip
      - references/planning-artifact-contracts.md
    done_when:
      - Strategic prototype prompt inputs and outputs are understood.
      - Tune/refinement prompt inputs and outputs are understood.
      - Validation input questions are listed without implementing policy changes.
  - task_id: M60-A002
    title: Define proto redesign artifact reference
    status: completed
    type: document
    owned_paths:
      - references/proto-redesign-artifact-contracts.md
    done_when:
      - Reference defines first-pass strategic prompt packs.
      - Reference defines tune/refined prompt packs.
      - Reference defines validation-present and validation-absent behavior.
      - Reference defines review evidence and decision handoff.
  - task_id: M60-A003
    title: Add proto prompt pack schema
    status: completed
    type: edit
    owned_paths:
      - schemas/proto-prompt-pack.schema.json
    done_when:
      - Schema captures source, strategic directions, prompt packs, tune deltas, and evidence.
      - Schema stays compatible with common contract validation.
  - task_id: M60-A004
    title: Validate contracts
    status: completed
    type: verify
    owned_paths:
      - references/proto-redesign-artifact-contracts.md
      - schemas/proto-prompt-pack.schema.json
      - changes/M60-proto-redesign-artifact-contracts/
    done_when:
      - "`npm run validate` passes."
