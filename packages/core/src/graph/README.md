# Contract Graph

This directory is reserved for graph operations over OpenWorkflow contracts.
Current graph creation still lives in `packages/core/src/workflow/initOpenWorkflow.ts`
because M06 is a behavior-preserving architecture migration.
