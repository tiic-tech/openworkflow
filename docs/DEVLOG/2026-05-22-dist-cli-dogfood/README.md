# 2026-05-22 Dist CLI Dogfood Devlog

This folder contains one focused devlog package about a small OpenWorkflow dogfood finding: inside the OpenWorkflow repo, the latest Agent-facing capability surface must be read from the repo-built dist CLI, not from a globally installed `openworkflow` binary.

## Files

- `devlog.md`: Chinese first-person devlog.
- `devlog_deck.html`: standalone interactive lesson deck.
- `analysis.json`: structured evidence and insight model.
- `artifacts/`: evidence notes and command-output excerpts.

## Scope

The scope is intentionally narrow. It does not summarize the whole 2026-05-22 development day. It captures one useful agent-operating lesson found while dogfooding the repo.

